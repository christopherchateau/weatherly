import React from 'react';

function Card(props) {
    console.log(props)
    return(
      <div>
      
      <h1>{props.data}</h1> 
      </div> 
  )
}

export default Card;

// the hour
// an image that reflects the weather
// the projected temperature

//Line 1062 in mock data Hourly forecast 

